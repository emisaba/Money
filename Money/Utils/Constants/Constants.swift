import UIKit
import FirebaseFirestore

let COLLECTION_INCOMES = Firestore.firestore().collection("incomes")
let COLLECTION_CATEGORIES = Firestore.firestore().collection("categories")
let COLLECTION_ITEMS = Firestore.firestore().collection("items")
let COLLECTION_BUDGETS = Firestore.firestore().collection("budget")
let COLLECTION_HISTORIES = Firestore.firestore().collection("histories")
let COLLECTION_SAVING = Firestore.firestore().collection("savings")
